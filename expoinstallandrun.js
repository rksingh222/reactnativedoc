expo init my-first-project
cd my-first-project
npm start
it will show the list of option to start on the simulator 
or on the real device by scanning the qr code
to run the app on device
install expo go 


Drawer Issue
-------------
When creating a drawer Navigator you might across an error related to reanimated 
after presets add plugins
presets: ['babel-preset-expo'],
    plugins: [
      "react-native-reanimated/plugin",
    ],
and then save the app
start the running process
